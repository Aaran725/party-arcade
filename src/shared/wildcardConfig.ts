// Shared between the Display (which generates the secret target) and the Controller
// (which shows the player the valid range) — a single source of truth so the two can
// never drift apart.
export const NUMBER_GUESS_RANGE = 100;
